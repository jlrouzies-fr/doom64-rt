// UE compatibility regression: wait for a live player, place its replacement
// barrel at the crosshair. Pair with rt_autofire_slot 5 and a long-enough
// rt_autofire_len (25 is safe) so a real rocket kills it after the weapon-raise
// animation and the normal damage/death path runs. Keep every command on one line:
// `wait` only defers the remainder of the same command buffer; separate
// command-line +commands are drained before map play.
wait 65; summon D64UE_ExplosiveBarrel
