// Metals as hand-labelled: the ORM blue channel and metallicDefault are honoured.
// Pair with metal-off.cfg -- the ONLY difference between the two arms is this
// cvar, so anything that moves between them is the metalness pass and nothing else.
rt_metallic 1
rt_refl_thresh 0
